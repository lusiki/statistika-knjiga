# Aktiviraj renv samo ako je projekt već inicijaliziran. Dok renv/ ne postoji,
# R sesija se otvara normalno (bez ovoga bi svaka sesija pucala na nedostajućem
# renv/activate.R). Inicijalizacija: Rscript scripts/init-renv.R
if (file.exists("renv/activate.R")) source("renv/activate.R")
