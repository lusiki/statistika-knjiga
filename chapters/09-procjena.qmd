---
title: "Procjena"
subtitle: "Interval pokazuje koliko je procjena precizna"
---

<!-- =====================================================================
  STATUS:  djelomični nacrt — argument, strukturni elementi i dovršen widget
  IZVOR:   tjedan 8, druga polovica, iznova izgrađeno
  OPSEG:   ~4000 riječi
  WIDGET:  w09 · Hvatač intervala
  FAZA:    3 (plan proizvodnje, notes/struktura-knjige.md)
  NAPOMENA: Jedan od četiri widgeta koji nose glavnu argumentaciju knjige.

  SADRŽAJ (iz plana):
  Točkaste procjene, bootstrap kao čitateljev vlastiti izum, intervali pouzdanosti konstruirani pa tek onda ispravno protumačeni, okvir postupka, preciznost nasuprot pouzdanosti.

  Kostur poglavlja i pravila pisanja: CLAUDE.md i STYLE.md.
  Kostur se ne mijenja bez zapisa u STYLE.md.
===================================================================== -->

```{r}
#| include: false
#| cache: false
source("R/setup.R")
```

::: {.chapter-meta}
| Vrijeme čitanja | Widget | Podaci | Preduvjet |
|---|---|---|---|
| 4 min | Hvatač intervala | simulacija | pogl. 8 |
:::

::: {.callout-vinjeta}
Cumming je zagovarao izvještavanje procjena i intervala kao središte
statističkog zaključivanja, umjesto oslanjanja na samu odluku o značajnosti
[@cumming2014]. Pomak mijenja pitanje koje postavljamo podacima. Umjesto
binarnog prolaza pitamo koliki je učinak i koliko je procjena precizna.

Interval ipak nije ukras oko točkaste procjene. Njegovo značenje dolazi iz
postupka koji bi kroz ponovljene uzorke stvarao intervale, među kojima neki
obuhvaćaju populacijsku vrijednost, a neki je promašuju.

Kako iz jednog opaženog intervala pošteno govoriti o vrijednosti koja ostaje
nepoznata?
:::

## Od točke prema rasponu

**Točkasta procjena** daje jednu vrijednost iz uzorka. Ona je najbolji sažeti
pogodak koji trenutačno imamo, ali drugi bi uzorak dao drugi rezultat. Interval
pouzdanosti toj procjeni dodaje raspon usklađen s njezinom uzoračkom
promjenjivošću.

Širi interval nije znak lošeg istraživača. On može pošteno pokazivati mali
uzorak ili vrlo promjenjive podatke. Uži interval označava veću preciznost pod
pretpostavkama postupka. Ne govori da su mjera, uzorak i model nepristrani.

Razina pouzdanosti pripada dugoročnom postupku. Kada bismo uzorkovanje i izradu
intervala ponavljali, unaprijed određeni udio intervala obuhvatio bi fiksnu
populacijsku vrijednost. Nakon što je jedan interval izračunat, njegova je
granica opažena, a populacijska vrijednost ostaje nepoznata.

## Bootstrap iz uzorka

Bootstrap ponovljeno izvlači opažanja s vraćanjem iz dostupnog uzorka, računa
željenu statistiku i slaže dobivene vrijednosti u raspodjelu [@efron1979].
Postupak tako približava pitanje što bi se dogodilo s procjenom kada bismo mogli
ponoviti prikupljanje sličnih podataka.

Njegova snaga je prilagodljivost. Može se primijeniti na medijan, razliku,
korelaciju i druge statistike za koje jednostavna formula nije pri ruci.
Njegova granica ostaje početni uzorak. Ako on ne predstavlja populaciju ili je
premalen da zabilježi važan dio raspodjele, resampling ponavlja tu prazninu.

## Interakcija — Hvatač intervala

Hvatač intervala prikazuje niz ponovljenih uzoraka i njihove intervale oko iste
populacijske vrijednosti. Većina je hvata, a neki je promašuju. Tek niz
intervala čini značenje razine pouzdanosti vidljivim. Simulacija koristi
normalnu populaciju poznate standardne devijacije kako bi odvojila logiku
obuhvata od procjene same standardne devijacije.

:::: {.content-visible when-format="html"}

::: {.widget-frame data-naslov="Hvatač intervala" data-oznaka="Widget 09.1 · interaktivno"}

```{ojs}
//| echo: false
viewof w09_par = Inputs.form({
  n: Inputs.range(
    [10, 200],
    {value: 40, step: 5, label: "Veličina uzorka"}
  ),
  razina: Inputs.select(
    ["90 %", "95 %", "99 %"],
    {value: "95 %", label: "Razina pouzdanosti"}
  ),
  k: Inputs.select(
    [20, 50, 100],
    {value: 50, label: "Broj intervala"}
  )
})
```

```{ojs}
//| echo: false
w09_intervali = {
  const rng = d3.randomLcg(908);
  const normalna = d3.randomNormal.source(rng)(0, 1);
  const kriticna = {
    "90 %": 1.645,
    "95 %": 1.960,
    "99 %": 2.576
  }[w09_par.razina];
  const pogreska = 1 / Math.sqrt(w09_par.n);

  return Array.from({length: w09_par.k}, (_, i) => {
    let zbroj = 0;
    for (let j = 0; j < w09_par.n; j += 1) zbroj += normalna();
    const procjena = zbroj / w09_par.n;
    const donja = procjena - kriticna * pogreska;
    const gornja = procjena + kriticna * pogreska;
    return {
      red: i + 1,
      procjena,
      donja,
      gornja,
      obuhvaca: donja <= 0 && gornja >= 0
    };
  });
}
```

```{ojs}
//| echo: false
//| label: fig-w09
//| fig-cap: "Intervali iz ponovljenih simuliranih uzoraka oko fiksne populacijske sredine. Istaknuti intervali promašuju okomitu ciljnu crtu."
//| fig-alt: "Pedeset vodoravnih intervala uglavnom presijeca okomitu crtu na nuli, dok je nekoliko intervala u cijelosti s jedne strane crte."
{
  const tinta = "var(--tok-ink)";
  const prigusena = "var(--tok-ink-mute)";
  const naglasak = "var(--tok-accent)";
  const obuhvaceni = w09_intervali.filter(d => d.obuhvaca);
  const promasaji = w09_intervali.filter(d => !d.obuhvaca);
  const celija = document.querySelector(".widget-frame .cell");
  const stilCelije = celija ? getComputedStyle(celija) : null;
  const lokalnaSirina = celija
    ? celija.clientWidth -
      parseFloat(stilCelije.paddingLeft) -
      parseFloat(stilCelije.paddingRight)
    : width;
  const sirinaGrafa = Math.max(280, Math.min(width, lokalnaSirina));
  const mobilno = sirinaGrafa < 480;

  return Plot.plot({
    width: sirinaGrafa,
    height: mobilno
      ? Math.max(500, Math.min(780, 170 + w09_par.k * 6))
      : Math.max(460, Math.min(740, 145 + w09_par.k * 5.8)),
    marginLeft: mobilno ? 28 : 24,
    marginRight: 20,
    marginBottom: mobilno ? 62 : 54,
    style: {
      background: "transparent",
      color: tinta,
      fontFamily: "var(--tok-font-mono)",
      fontSize: mobilno ? "15px" : "13px"
    },
    x: {
      label: "Interval procjene",
      domain: [-1.8, 1.8],
      nice: false,
      grid: true
    },
    y: {axis: null, reverse: true},
    marks: [
      Plot.ruleX([0], {
        stroke: tinta,
        strokeWidth: 1.4
      }),
      Plot.ruleY(obuhvaceni, {
        y: "red",
        x1: "donja",
        x2: "gornja",
        stroke: prigusena,
        strokeWidth: 1.2
      }),
      Plot.ruleY(promasaji, {
        y: "red",
        x1: "donja",
        x2: "gornja",
        stroke: naglasak,
        strokeWidth: 2.4
      }),
      Plot.dot(obuhvaceni, {
        x: "procjena",
        y: "red",
        fill: tinta,
        r: 2.6
      }),
      Plot.dot(promasaji, {
        x: "procjena",
        y: "red",
        fill: naglasak,
        r: 2.6
      }),
      Plot.text(promasaji, {
        x: "procjena",
        y: "red",
        text: () => "×",
        fill: naglasak,
        fontSize: mobilno ? 15 : 13,
        dy: 1
      })
    ]
  });
}
```

```{ojs}
//| echo: false
{
  const obuhvaceni = d3.sum(w09_intervali, d => d.obuhvaca ? 1 : 0);
  const udio = (100 * obuhvaceni / w09_intervali.length).toLocaleString(
    "hr-HR",
    {minimumFractionDigits: 1, maximumFractionDigits: 1}
  );
  return html`<p class="widget-foot" role="status" aria-live="polite">Cilj obuhvaća ${obuhvaceni} od ${w09_intervali.length} intervala, odnosno ${udio} %.</p>`;
}
```

:::

::::

::: {.content-visible when-format="pdf"}

```{r}
#| echo: false
#| label: fig-w09-print
#| fig-cap: "Pedeset intervala iz ponovljenih simuliranih uzoraka i fiksna populacijska sredina. Križići označuju intervale koji promašuju cilj."
#| fig-alt: "Pedeset vodoravnih intervala većinom presijeca okomitu crtu na nuli, a intervali koji je ne presijecaju označeni su križićem."
#| fig-width: 6.6
#| fig-height: 6.8
set.seed(909)

w09_tisak <- tibble(
  red = 1:50,
  procjena = replicate(50, mean(rnorm(40))),
  pogreska = 1 / sqrt(40)
) |>
  mutate(
    donja = procjena - 1.96 * pogreska,
    gornja = procjena + 1.96 * pogreska,
    ishod = if_else(
      donja <= 0 & gornja >= 0,
      "Obuhvaća",
      "Promašuje"
    ),
    ishod = factor(ishod, levels = c("Obuhvaća", "Promašuje"))
  )

ggplot(
  w09_tisak,
  aes(y = red, colour = ishod, shape = ishod)
) +
  geom_vline(
    xintercept = 0,
    colour = tok_boja("ink"),
    linewidth = 0.5
  ) +
  geom_segment(
    aes(x = donja, xend = gornja, yend = red),
    linewidth = 0.65
  ) +
  geom_point(aes(x = procjena), size = 1.8) +
  skala_naglasak(
    "Promašuje",
    razine = levels(w09_tisak$ishod),
    tip = "colour"
  ) +
  scale_shape_manual(values = c(16, 4)) +
  scale_y_reverse(breaks = NULL) +
  scale_x_continuous(
    limits = c(-1.8, 1.8),
    breaks = seq(-1.5, 1.5, by = 0.5),
    labels = hr_broj,
    expand = expansion(mult = 0)
  ) +
  labs(x = "Interval procjene", y = NULL) +
  theme_knjiga(base_size = 13, mreza = "x")
```

:::

**Što isprobati.**

1. Zadržite pedeset intervala i prebrojite one koji ne sijeku okomitu ciljnu crtu.
2. Povećajte broj intervala na sto bez promjene veličine uzorka ili razine pouzdanosti.
3. Usporedite širinu intervala pri uzorcima veličine dvadeset i sto.
4. Promijenite razinu pouzdanosti s devedeset na devedeset i devet posto te opišite odnos širine i obuhvata.

::: {.callout-divljina}
**Procjena prije odluke.** Pristup „novih statistika" stavlja veličinu učinka,
interval i metaanalitičko povezivanje nalaza ispred binarne odluke o
značajnosti [@cumming2014].

Interval ne jamči da je istina blizu sredine ni da je istraživanje valjano.
Njegova vrijednost je u tome što čitatelju pokazuje koje su veličine još
usklađene s postupkom i koliko prostora ostaje za znanstvenu nesigurnost.
:::

::: {.callout-model}
Asistent može bootstrapirati gotovo svaku statistiku, ali treba provjeriti
uzorkuje li s vraćanjem, čuva li strukturu uparenih ili grupiranih podataka i
ponavlja li dovoljno puta. Modeli često daju pogrešno probabilističko značenje
već izračunatom intervalu.

> Izračunaj točkastu procjenu i bootstrap interval. Sačuvaj strukturu podataka,
> opiši postupak resamplinga i interpretiraj razinu pouzdanosti kao svojstvo
> ponovljenog postupka.
:::

::: {.callout-greska}
Bootstrap raspodjela je približno simetrična i interval je uredno izračunat iz
njezinih krajeva. Postoji devedesetpetpostotna vjerojatnost da se fiksna
populacijska vrijednost nalazi unutar upravo ovog opaženog intervala.

::: {.content-visible when-profile="kolegij"}
Greška je pripisivanje vjerojatnosti fiksnom parametru nakon izračuna
frekventističkog intervala. Razina pouzdanosti opisuje dugoročni udio intervala
koji obuhvaćaju parametar.
:::
:::

## Razrađeni primjer

Simulirani uzorak sadrži ocjene povjerenja. Zanimaju nas medijan i njegova
nesigurnost jer raspodjela nije savršeno simetrična. Bootstrap uzorci ponovno
izvlače opažene ocjene s vraćanjem i za svaki izračunavaju medijan.

```{r}
#| label: tbl-bootstrap-interval
#| tbl-cap: "Bootstrap procjena medijana simuliranih ocjena. Izrada autora."

sim_povjerenje <- tibble(
  ocjena = sample(1:10, 80, replace = TRUE,
                  prob = c(2, 3, 5, 7, 10, 12, 14, 12, 9, 6))
)

boot_medijani <- replicate(
  3000,
  median(sample(sim_povjerenje$ocjena, replace = TRUE))
)

w09_tablica <- tibble(
  `Procjena medijana` = hr_broj(median(sim_povjerenje$ocjena), 1),
  `Donja granica` = hr_broj(quantile(boot_medijani, 0.025), 1),
  `Gornja granica` = hr_broj(quantile(boot_medijani, 0.975), 1)
)

if (knitr::is_html_output()) {
  knitr::kable(
    w09_tablica,
    format = "html",
    align = c("r", "r", "r"),
    table.attr = paste(
      'class="table table-sm"',
      'data-quarto-disable-processing="true"'
    )
  )
} else {
  knitr::kable(
    w09_tablica,
    format = "pipe",
    align = c("r", "r", "r")
  )
}
```

Tablica je rezultat simuliranog nastavnog primjera, a ne nalaz o stvarnom
povjerenju. Ona pokazuje redoslijed izvještavanja. Najprije dolazi procjena,
zatim raspon nesigurnosti i naposljetku ograničenje uzorka i mjere.

## Sažetak {.sazetak}

Procjena počinje jednom vrijednošću, ali ne završava njome. Interval prikazuje
preciznost postupka i mora se tumačiti kroz ponovljeno uzorkovanje. Bootstrap
približava tu promjenjivost resamplingom dostupnog uzorka, bez čarobnog
popravljanja njegove pristranosti. Sljedeće poglavlje uvodi testiranje kao još
jedan način usporedbe opaženog rezultata s raspodjelom mogućih rezultata.

## Pojmovi {.pojmovi}

točkasta procjena (*point estimate*), interval pouzdanosti (*confidence
interval*), preciznost (*precision*), bootstrap (*bootstrap*), resampling
(*resampling*), parametar (*parameter*)

## Zadaci

### Konceptualni {.zadaci-razina}

Objasnite zašto razina pouzdanosti pripada postupku, a ne nepoznatom parametru
nakon izračuna intervala. Predajte jedan odlomak.

### Računski {.zadaci-razina}

Upotrijebite `sim_povjerenje`. Bootstrapirajte aritmetičku sredinu i medijan te
predajte usporednu tablicu intervala.

### Kritički {.zadaci-razina}

Prosudite zašto izvještaj usmjeren na procjenu i interval daje više informacija
od same odluke o značajnosti [@cumming2014]. Predajte tri rečenice.

### Revizija modela {.zadaci-razina}

Ocijenite modelsku interpretaciju iz okvira. Imenujte točan postupak, jednu
pogrešnu rečenicu i frekventistički ispravnu zamjenu.
