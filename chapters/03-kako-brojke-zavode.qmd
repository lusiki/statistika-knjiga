---
title: "Kako brojke zavode"
subtitle: "Četiri provjere prije nego povjerujemo brojci"
---

<!-- =====================================================================
  STATUS:  cjelovit nacrt — argument, widget i statični blizanac
  IZVOR:   novo poglavlje, dijelom sastavljeno iz fragmenata tjedna 1
  OPSEG:   ~5000 riječi
  WIDGET:  w03 · Istraživač margine pogreške
  FAZA:    4 (plan proizvodnje, notes/struktura-knjige.md)
  NAPOMENA: Javno lice knjige. Jedan od tri stupa suvremenog identiteta (uz 12 i 17).

  SADRŽAJ (iz plana):
  Obmanjujuće osi i grafički prijestupi, zanemarivanje baznih stopa, postotci nasuprot postotnim bodovima, margine pogreške u naslovima, biranje trešanja, kontrolna lista za čitanje ankete, te odjeljak o prosuđivanju AI-generirane analize (izmišljene brojke, lažna preciznost, samouvjerena besmislica, sintetički mediji, pitanje podrijetla). Završava prvim alatom čitatelja, protokolom skeptika.

  Kostur poglavlja i pravila pisanja: CLAUDE.md i STYLE.md.
  Kostur se ne mijenja bez zapisa u STYLE.md.
===================================================================== -->

```{r}
#| include: false
#| cache: false
source("R/setup.R")
```

::: {.chapter-meta}
| Vrijeme čitanja | Widget | Podaci | Preduvjet |
|---|---|---|---|
| 4 min | Istraživač margine pogreške | simulacija | pogl. 1 i 2 |
:::

::: {.callout-vinjeta}
Američko statističko udruženje objavilo je izjavu o p-vrijednostima nakon
desetljeća u kojima se jedan prag često koristio kao zamjena za znanstveni sud
[@wasserstein2016]. Problem nije bio u tome što su istraživači zaboravili
računati. Problem je nastajao kada je jedan broj preuzimao značenje koje mu
postupak nije davao.

Sličan prijenos značenja događa se u naslovima anketa, postocima bez nazivnika
i grafovima kojima odabrani raspon osi pretvara malu promjenu u dramatičan lom.
Broj može biti točan, a prikaz ipak voditi prema zaključku koji podaci ne nose.

Kako razlikovati računsku pogrešku od mnogo češće pogreške u okviru, usporedbi
i jeziku?
:::

## Četiri provjere jedne tvrdnje

Prva provjera traži izvor. Tvrdnja koja navodi istraživanje mora omogućiti
pronalaženje izvornog izvještaja, tablice ili skupa podataka. Poveznica na
drugi članak nije podrijetlo brojke, a navođenje ustanove bez godine i
istraživanja nije dovoljno da bi se nalaz provjerio.

Druga provjera traži nazivnik. Porast od pedeset posto može značiti prijelaz s
dva slučaja na tri ili s dva milijuna na tri milijuna. Postotak opisuje omjer,
dok **postotni bod** opisuje razliku između dvaju postotaka. Njihova zamjena
može višestruko povećati dojam promjene iako je aritmetika pojedinačnih brojeva
točna.

Treća provjera traži usporedbu. Graf bez zajedničke nule nije automatski
nepošten, ali skraćena os mora biti vidljiva i opravdana. Odabir početnog
razdoblja, izostavljanje jedne skupine ili isticanje samo povoljnog ishoda
mijenja priču. Čitatelj zato pita koje bi razumno drugačije uokvirivanje
pokazalo isti podatak.

Četvrta provjera traži neizvjesnost. Rezultat ankete nije svojstvo uzorka koje
će se bez promjene ponoviti u populaciji. Margina pogreške opisuje samo
uzoračku promjenjivost pod određenim pretpostavkama. Ne obuhvaća pristran
okvir, neodaziv, loše pitanje ni naknadno biranje najzanimljivijeg rezultata.

## Protokol skeptičnog čitanja

Skeptičnost nije automatsko odbacivanje. Ona usporava prijelaz od podatka prema
tvrdnji. Najprije utvrđujemo tko je proizveo broj i za koju svrhu. Zatim
provjeravamo jedinicu analize, nazivnik, vremenski okvir i usporednu skupinu.
Tek tada procjenjujemo koliko neizvjesnost i dizajn dopuštaju zaključak.

Isti protokol vrijedi za ljudski i strojno proizveden tekst. Asistent može
izmisliti izvor, popuniti ćeliju koja nedostaje ili zaokružiti rezultat do
lažne preciznosti. Uvjerljiv stil nije dokaz podrijetla. Svaka brojka mora
ostaviti trag do podataka ili postupka iz kojeg je nastala.

## Interakcija — Istraživač margine pogreške

Istraživač prikazuje približnu marginu pogreške uzorka pri različitim
veličinama i procijenjenim udjelima. Zaseban klizač uvodi poznatu sustavnu
pristranost. Interval se tada može sužavati oko precizno procijenjene pogrešne
vrijednosti.

:::: {.content-visible when-format="html"}
::: {.widget-frame data-naslov="Istraživač margine pogreške" data-oznaka="Widget 03.1 · interaktivno"}
```{ojs}
//| echo: false
viewof w03_par = Inputs.form({
  n: Inputs.range(
    [100, 3000],
    {value: 1000, step: 100, label: "Veličina uzorka"}
  ),
  udio: Inputs.range(
    [10, 90],
    {value: 52, step: 1, label: "Procijenjeni udio u postocima"}
  ),
  pristranost: Inputs.range(
    [-10, 10],
    {
      value: 0,
      step: 1,
      label: "Pristranost procjene u postotnim bodovima"
    }
  )
})
```

```{ojs}
//| echo: false
w03_tema = Generators.observe(obavijesti => {
  let revizija = 0;
  const osvjezi = () => obavijesti(++revizija);
  const promatrac = new MutationObserver(osvjezi);
  const postavke = {
    attributes: true,
    attributeFilter: ["class", "data-bs-theme"]
  };
  promatrac.observe(document.documentElement, postavke);
  promatrac.observe(document.body, postavke);
  osvjezi();
  return () => promatrac.disconnect();
})
```

```{ojs}
//| echo: false
//| label: fig-w03
//| fig-cap: "Približna margina pogreške i položaj pretpostavljene istinite vrijednosti u konstruiranoj anketi."
//| fig-alt: "Pri početnom uzorku od 1000 i procjeni od 52 posto margina iznosi približno 3,1 postotni bod, a procjena se poklapa s pretpostavljenom istinitom vrijednošću."
{
  w03_tema;
  const css = getComputedStyle(document.documentElement);
  const tinta = css.getPropertyValue("--tok-data-1").trim();
  const istinaBoja = css.getPropertyValue("--tok-data-2").trim();
  const intervalBoja = css.getPropertyValue("--tok-data-4").trim();
  const prigusena = css.getPropertyValue("--tok-ink-mute").trim();
  const sans = css.getPropertyValue("--tok-font-sans").trim();
  const postotak = new Intl.NumberFormat("hr-HR", {
    style: "percent",
    minimumFractionDigits: 0,
    maximumFractionDigits: 1
  });
  const decimalni = new Intl.NumberFormat("hr-HR", {
    minimumFractionDigits: 1,
    maximumFractionDigits: 1
  });

  const procjena = w03_par.udio / 100;
  const pristranost = w03_par.pristranost / 100;
  const istina = procjena - pristranost;
  const margina = 1.96 * Math.sqrt(
    procjena * (1 - procjena) / w03_par.n
  );
  const donja = Math.max(0, procjena - margina);
  const gornja = Math.min(1, procjena + margina);
  const poklapanje = Math.abs(pristranost) < 1e-9;

  const marks = [
    Plot.ruleY(
      [{y: 0, donja, gornja}],
      {
        y: "y",
        x1: "donja",
        x2: "gornja",
        stroke: intervalBoja,
        strokeWidth: 8,
        strokeLinecap: "round"
      }
    ),
    Plot.dot(
      [
        {x: donja, y: 0},
        {x: gornja, y: 0}
      ],
      {x: "x", y: "y", r: 3.6, fill: intervalBoja}
    ),
    Plot.dot(
      [{x: procjena, y: 0}],
      {x: "x", y: "y", r: 7, fill: tinta}
    ),
    Plot.text(
      [{
        x: (donja + gornja) / 2,
        y: 0.26,
        oznaka: `približna margina ± ${decimalni.format(100 * margina)} boda`
      }],
      {x: "x", y: "y", text: "oznaka", fill: prigusena}
    )
  ];

  if (poklapanje) {
    marks.push(
      Plot.text(
        [{
          x: procjena,
          y: -0.25,
          oznaka: `procjena i pretpostavljena istina ${postotak.format(procjena)}`
        }],
        {x: "x", y: "y", text: "oznaka", fill: prigusena}
      )
    );
  } else {
    marks.push(
      Plot.dot(
        [{x: istina, y: 0}],
        {
          x: "x",
          y: "y",
          r: 6.5,
          symbol: "diamond",
          fill: istinaBoja
        }
      ),
      Plot.text(
        [
          {
            x: procjena,
            y: -0.25,
            oznaka: `procjena ${postotak.format(procjena)}`
          },
          {
            x: istina,
            y: 0.42,
            oznaka: `pretpostavljena istina ${postotak.format(istina)}`
          }
        ],
        {x: "x", y: "y", text: "oznaka", fill: prigusena}
      )
    );
  }

  const celija = document.querySelector(".widget-frame .cell");
  const stilCelije = celija ? getComputedStyle(celija) : null;
  const lokalnaSirina = celija
    ? celija.clientWidth -
      parseFloat(stilCelije.paddingLeft) -
      parseFloat(stilCelije.paddingRight)
    : width;
  const sirinaGrafa = Math.max(240, Math.min(width, lokalnaSirina));
  const usko = sirinaGrafa < 480;
  return Plot.plot({
    width: sirinaGrafa,
    height: usko ? 310 : 260,
    marginLeft: usko ? 38 : 42,
    marginRight: usko ? 16 : 28,
    marginTop: usko ? 48 : 30,
    marginBottom: 48,
    style: {
      background: "transparent",
      fontFamily: sans,
      fontSize: usko ? "12px" : "13px"
    },
    x: {
      label: "Udio",
      domain: [0, 1],
      tickFormat: d => postotak.format(d),
      grid: true
    },
    y: {axis: null, domain: [-0.45, 0.55]},
    marks
  });
}
```
:::
::::

::: {.content-visible when-format="pdf"}
```{r}
#| echo: false
#| label: fig-w03-print
#| fig-cap: "Tri stanja pokazuju učinak veličine uzorka i sustavne pristranosti."
#| fig-alt: "Uzorak od 250 daje širi interval od uzorka od 1000. U trećem stanju uzorak ostaje 1000, ali pristranost od šest postotnih bodova pomiče pretpostavljenu istinitu vrijednost izvan uskog intervala."
w03_stanja <- tibble(
  Stanje = c(
    "n = 250, bez pristranosti",
    "n = 1 000, bez pristranosti",
    "n = 1 000, pristranost 6 bodova"
  ),
  n = c(250, 1000, 1000),
  Procjena = c(0.52, 0.52, 0.52),
  Pristranost = c(0, 0, 0.06)
) |>
  mutate(
    Istina = Procjena - Pristranost,
    Margina = 1.96 * sqrt(Procjena * (1 - Procjena) / n),
    Donja = Procjena - Margina,
    Gornja = Procjena + Margina,
    Stanje = factor(Stanje, levels = rev(Stanje))
  )

w03_tocke <- w03_stanja |>
  select(Stanje, Procjena, `Pretpostavljena istina` = Istina) |>
  pivot_longer(
    cols = -Stanje,
    names_to = "Mjera",
    values_to = "Vrijednost"
  )

ggplot(w03_stanja, aes(y = Stanje)) +
  geom_segment(
    aes(x = Donja, xend = Gornja, yend = Stanje),
    colour = sivo[[4]],
    linewidth = 3.2,
    lineend = "round"
  ) +
  geom_point(
    data = w03_tocke,
    aes(x = Vrijednost, colour = Mjera, shape = Mjera),
    size = 2.7
  ) +
  scale_color_manual(values = c(sivo[[1]], sivo[[2]])) +
  scale_shape_manual(values = c(16, 4)) +
  scale_x_continuous(
    labels = function(x) paste0(hr_broj(100 * x, 0), " %"),
    limits = c(0.42, 0.60)
  ) +
  labs(x = "Udio", y = NULL) +
  theme(legend.position = "bottom")
```
:::

**Što isprobati.**

1. Povećavajte uzorak i promatrajte brzinu sužavanja margine.
2. Zadržite uzorak jednakim, a promijenite procijenjeni udio.
3. Uključite sustavnu pristranost i provjerite zašto uži interval ne mora biti
   bliži istini.

::: {.callout-divljina}
**Prag koji nije presuda.** Izjava Američkog statističkog udruženja naglasila
je da p-vrijednost sama ne mjeri veličinu ni važnost učinka i ne određuje treba
li rezultat smatrati znanstveno vrijednim [@wasserstein2016].

Naslov koji istraživanje svodi na „dokazano" ili „nije dokazano" uklanja
procjenu, neizvjesnost i dizajn. Broj ostaje vidljiv, a upravo informacije
potrebne za njegovo tumačenje nestaju.
:::

::: {.callout-model}
Asistent je koristan kao strogi čitatelj ako dobije izvornu tablicu i jasnu
zabranu nadopunjavanja nedostajućih podataka. Treba tražiti da odvoji provjeru
aritmetike od procjene dizajna i jezika. Nakon odgovora ručno se otvara svaki
navedeni izvor i provjerava postoji li broj u njemu.

> Rastavi ovu statističku tvrdnju na izvor, brojnik, nazivnik, usporedbu,
> neizvjesnost i dopušten zaključak. Ne dopunjuj podatke koji nisu priloženi i
> jasno označi što nije moguće provjeriti.
:::

::: {.callout-greska}
Anketa pokazuje vodstvo jedne opcije, ali se intervali procjena dviju opcija
preklapaju. Zbog preklapanja možemo zaključiti da među njima sigurno nema
razlike. Uzorak je opisan, a postoci se zbrajaju do cjeline.

::: {.content-visible when-profile="kolegij"}
Greška je zaključak da preklapanje intervala dokazuje nepostojanje razlike.
Odnos dviju procjena mora se procijeniti izravno, uz dizajn ankete i ovisnost
procjena, a ne samo pogledom na dva odvojena intervala.
:::
:::

## Razrađeni primjer

Simuliramo dvije ankete o istoj podršci, jednu s manjim i jednu s većim
uzorkom. Obje su pošteno uzorkovane iz iste zamišljene populacije. Cilj nije
dobiti stvarni izborni rezultat, nego vidjeti što se mijenja kada povećamo broj
opažanja.

```{r}
#| label: tbl-margina-simulacija
#| tbl-cap: "Simulirane procjene pri dvjema veličinama uzorka. Izrada autora."

sim_ankete <- tibble(
  uzorak = c("manji", "veći"),
  n = c(250, 1000),
  podrska = c(0.52, 0.52)
) |>
  mutate(
    standardna_pogreska = sqrt(podrska * (1 - podrska) / n),
    margina = 1.96 * standardna_pogreska,
    prikaz = paste0("± ", hr_broj(100 * margina, 1), " %")
  )

sim_ankete |>
  select(Uzorak = uzorak, `Veličina uzorka` = n, `Margina pogreške` = prikaz) |>
  knitr::kable(align = c("l", "r", "r"))
```

Veći uzorak daje užu marginu, ali obje ankete dijele pretpostavku da je
uzorkovanje nepristrano. Kada bi okvir isključio dio populacije, tablica bi i
dalje mogla pokazivati veliku računsku preciznost. Strog prikaz zato uz marginu
navodi način odabira ispitanika, datum, formulaciju pitanja i naručitelja.

## Sažetak {.sazetak}

Brojke zavode kada izgube izvor, nazivnik, usporedbu ili neizvjesnost. Pogreška
ne mora biti u računu jer često nastaje u izboru prikaza i jezika kojim se
rezultat pretvara u tvrdnju. Skeptični protokol jednak je za novinski naslov,
znanstveni sažetak i odgovor modela. U sljedećem dijelu knjige isti se zahtjev
primjenjuje na sažimanje i vizualizaciju vlastitih podataka.

## Pojmovi {.pojmovi}

nazivnik (*denominator*), postotni bod (*percentage point*), margina pogreške
(*margin of error*), pristranost (*bias*), lažna preciznost (*false
precision*), podrijetlo podatka (*data provenance*)

## Zadaci

### Konceptualni {.zadaci-razina}

Objasnite razliku između pedesetpostotnog rasta i rasta od pedeset postotnih
bodova. Predajte vlastiti primjer bez stvarnih empirijskih tvrdnji.

### Računski {.zadaci-razina}

Upotrijebite simulirane podatke `sim_ankete`. Dodajte treću veličinu uzorka i
predajte tablicu s pripadajućom marginom pogreške.

### Kritički {.zadaci-razina}

Pročitajte izjavu o p-vrijednostima i izdvojite jednu tvrdnju koju ona
dopušta te jednu koju izričito ne dopušta [@wasserstein2016]. Predajte dvije
rečenice i citat izvora.

### Revizija modela {.zadaci-razina}

Ocijenite analizu modela iz okvira iznad. Odvojite točne provjere od jedne
pogrešne interpretacije i napišite provjerljiviju zamjenu.
