# WD-C15 — Chapter 15 vertical slice

Date: 2026-08-17

Packet: `WD-C15`

Branch: `revision/comprehensive-review`

Claim base: `4e482de7fd8c805d5349ed9677bc1e9898684eb9`

## Claim and boundaries

The packet was claimed from a clean tree after
`scripts/check-review-workflow.R` passed. `WD-C15` is the sole active write
packet. Its write scope is `chapters/15-vise-grupa.qmd`, the regenerated
`data/concept-graph.json`, the w15 source fingerprints in `data/widgets.json`,
this evidence bundle, the six read-only critic reports and synthesis, and the
three workflow control files. Chapter 14, Chapter 16, shared registries,
datasets, AI exports and render artifacts are outside the write scope.

The prerequisites `C14` and `P1A-C15` are accepted. The ratified Chapter 15
spine from `P2-SPINE-V` governs the revision. Chapter 6 remains deliberately at
`draft`, Chapters 7–12 retain their reaccepted states, and `C15` remains a
separate acceptance gate with the exact author-reply stop.

## Incoming handoff

The complete target scan found one delivery to this packet:
`H-P3-ESS-001`. It was acknowledged and consumed before claim. ESS Round 11
edition 3.0 remains an optional, reader-owned, portal-mediated extension with
no local bytes, checksum, empirical result, edition-parity or permission
claim. Mandatory offline work uses the promoted CC BY 4.0 files
`data/populacija-medija.csv` and
`data/populacija-medija-agregat.csv`. The open rights ask
`OA-G-A3-ESS-RIGHTS` continues to prohibit bundling.

## Governed obligations

- preserve the ratified spine and canonical Chapter 14 terminology;
- lead with magnitude, uncertainty and multiplicity consequences rather than
  treating the omnibus decision as the payoff;
- keep the variance ratio as a rough screen, never proof of equal variances;
- include an operational dependence stop rule;
- provide one bounded, non-executed suspect-code diagnosis task;
- provide a genuine reach-back task that cannot be solved from Chapter 15
  alone;
- revalidate inherited D02 as the distinction between the same point estimate
  and different uncertainty;
- verify source/data provenance, numerical results, figure introductions,
  widget/static-twin parity, print completion and multi-format rendering;
- run one final six-critic panel on a single material source state and resolve
  every fatal or major finding before closeout.

## Evidence log

### Final material state

The source held for the final panel has:

- SHA-256 `fd8337520901df9bbce56e25880f12b889fd54d46e4c1bb3e8f17da3ca49d813`;
- git blob `aa644049bacb62e7fc05ab75d3b6157b83165b96`;
- 10 top-level sections, 2,527 prose words and body-evenness coefficient 0.21;
- exactly three definition blocks, one conceptual figure with one HTML and one
  print source, both AI boxes and all four exercise tiers.

The material revision leads with the cost of multiple opportunities for error,
then interprets eta-squared before using F as an omnibus gate. It reports
substantive contrasts with simultaneous intervals, names planned and post-hoc
questions separately, and refuses to equate a non-separated pair with equal
groups. The classical and Welch routes preserve the same group means and
contrasts as point estimates while computing uncertainty differently.

The variance ratio is now only a rough screen read with distributions,
residuals, group sizes and design; it is never assumption proof. Repeated,
nested, household, organisational, place-based or network-linked rows stop
ordinary independent-row inference before any choice among classical, Welch
or rank procedures. This records the Chapter 15 slice of
`R22-C14-C16-dependence` without changing that multi-chapter item's owner or
status.

`R23-C15-suspect-code` is represented by a six-line, non-executed code artifact
with one inferential error: `p.adjust.method = "none"` leaves the ten pairwise
tests without familywise protection. The learner diagnoses fatal and useful
parts and writes no replacement code. `R35-REACHBACK-15` uses the licensed
offline aggregate and requires retrieval of descriptive-versus-causal claims
from Chapter 2 and the smallest effect size of interest from Chapter 11. Its
instructor-only canonical closure fixes the computed population difference and
the causal boundary.

### Clean-session numerical reproduction

The final source was reproduced in a fresh R process from the governed
`populacija_medija` object and seeds 1515–1517. The sample has 300 rows and five
groups. The classical model gives:

- `F = 8.381812957409`, numerator df 4, denominator df 295 and
  `p = 2.04182135962458e-06`;
- between-group mean square `28.461272095959`, within-group mean square
  `3.395598570450`, between-group sum of squares `113.845088383838` and
  within-group sum of squares `1001.701578282829`;
- eta-squared `0.102053183238` and omega-squared `0.089604885986`.

The illustrative Tukey contrast TV minus social media is `1.585714285714`,
with simultaneous 95% interval `0.707372515223` to `2.464056056205` and
adjusted `p = 1.20295215655375e-05`. Exactly four of ten contrasts are
separated and all four involve social media. Print minus social media is the
numerically largest contrast at `1.625892857143`, so the final prose makes no
false superlative claim. Print minus TV is `0.040178571429`, with interval
`-1.057748641685` to `1.138105784542`; its width is `2.195854426227`, so the
prose correctly distinguishes non-separation from equality.

The largest-to-smallest variance ratio is `1.432656498313`. Welch gives
`F = 7.320807631166`, denominator df `112.425284937609` and
`p = 2.81679627812019e-05`; Kruskal–Wallis gives `29.820424479989` and
`p = 5.32418421217587e-06`. The null simulations return 27.4% for at least one
unadjusted pairwise false positive, 4.25% for the omnibus test and 40.4% for ten
independent tests, against the independence formula's 40.126306076162%.

The five-row offline aggregate independently gives TV minus social media
`1.297259749250`, matching the instructor-only closure. No ESS result was
computed, stated or implied.

### Concepts, data and deterministic gates

Moving the interaction before the eta-squared and F definitions changed
concept co-occurrence, so the generated graph was rebuilt inside the packet.
It has 49 nodes, 608 total edges, 249 displayed co-occurrence edges and 45
definition edges; its SHA-256 is
`aea738c649a213d967a1456b3ddcd47f338a69d349fa124929fe517b2ae6281e`.

The final w15 widget fingerprints are
`f9f2533eb899673a6a26ee494a149f0512c3f70d68b962df0fa27f1d54b13a73`
for OJS and
`b8b3acf152646f0a8864f9a3eb64eb67636929e5c0eb8692a670af0e20c9dfc1`
for the R print twin. The complete `data/widgets.json` SHA-256 is
`901bf9924d3c91833ab8b80baec11ae3ee53d81917745a8ecb56ee3211edaceb`.

The following gates pass on the held material state:

- STYLE linter with zero candidates and the manual top-to-bottom Croatian
  sweep; structure rhythm, chapter scan and figure-introduction detector;
- all 19 ratified chapter spines, canonical terminology, citations and fresh
  concept integrity;
- catalogue and ESS portal boundaries, including 20 packages, five promoted
  packages, no rights-permission claim, no local ESS file or checksum and the
  open rights ask;
- assessment architecture with no assessed code production;
- all 17 widget contracts and parity pairs, six exact and eleven
  distributional;
- data integrity for 50,300 generated rows, all 21 governed snapshots and all
  44 fail-closed data fixtures;
- full manuscript integrity, design-token consistency and the comprehensive
  review workflow validator.

The recurring `renv` warning about one stale package directory named `windows`
and the project sync status did not skip or change any gate. It is pre-existing
and outside this packet.

### Multi-format validation

An isolated detached worktree at render snapshot
`88a99e88d2b1f5f50311eebe5df6832e08da73ed`, whose Chapter 15 git blob was the
exact held blob, used the primary checkout's existing ignored `renv` library.
The targeted HTML render executed all 13 cells and produced a 178,751-byte page
with SHA-256
`6dd95e5a0d7f3aa21e794c1633147a3d42b4a57e122bf5bd787949d602d448ad`.

The approved full PDF wrapper returned `PDF_BUILD_OK`. The PDF has 2,765,235
bytes and SHA-256
`27d40997601e7abc4924462c03dbd86d7fb4fe1a100479faaa8c49746f2bbf7d`.
The full DOCX wrapper returned exit 0, restored every temporary gate, and
produced a 1,589,391-byte document with SHA-256
`bfac7f17c9ffeb4cb240a5c711ee885c14efe9e2baff7fe6a2b4ffff59b982a7`.
The wrappers restored their temporary source gates and the Chapter 15 blob
still matched exactly after rendering. Only isolated generated outputs and
cache entries differed in that disposable worktree; it was then removed.
No HTML, PDF, DOCX, AI export, `_freeze/` or intermediate render file entered
the packet checkout.

Two exploratory HTML invocations in the primary checkout were not used as
completion evidence. An external output directory triggered Quarto's resource
walker `RangeError`; stdout mode was terminated after the same abnormal delay.
The pre-render hook's known generated AI-export paths were restored exactly to
the clean claim state and its untracked intermediates were removed before the
successful isolated renders.

### Six-critic panel and closeout

The first diagnostic panel addressed earlier blob
`4b62c9bd5099fb298753286cfa9bd72c10cf0f78` and exposed six major and fourteen
minor lens-level issues. It was not used as final evidence. The revision
resolved every major issue before the mandatory rerun.

All six independent final critics then reread the complete held blob
`aa644049bacb62e7fc05ab75d3b6157b83165b96` in read-only mode and confirmed the
same SHA-256 before and after their review. Methods, skepticism, pedagogy,
evidence and structure report zero fatal, zero major and zero minor findings.
Style reports zero fatal, zero major and four local minor polish records. The
panel total is therefore zero fatal, zero major and four nonblocking minor
records. Useful-improvement notes are recorded separately and are not counted
as minor findings.

The four style records concern one metareference, two clipped widget labels,
one inverted point-estimate sentence and the rhythm of the summary. None
changes a statistical claim, result, assumption, citation, exercise answer,
widget contract or fixed chapter structure. Editing after the panel would
invalidate its common source identity, so these records pass visibly to the
separate C15 author gate for explicit accept-or-return disposition.

The six reports and synthesis are:

- `notes/reports/wd-c15-critic-methods-2026-08-17.md`;
- `notes/reports/wd-c15-critic-skeptic-2026-08-17.md`;
- `notes/reports/wd-c15-critic-pedagogy-2026-08-17.md`;
- `notes/reports/wd-c15-critic-evidence-2026-08-17.md`;
- `notes/reports/wd-c15-critic-style-2026-08-17.md`;
- `notes/reports/wd-c15-critic-structure-2026-08-17.md`;
- `notes/reports/wd-c15-six-critic-synthesis-2026-08-17.md`.

No new future-relevant effect lacks an existing owner. The four minors pass
directly to C15, the ESS boundary remains owned by its existing handoff and
rights ask, the Chapter 16 bridge remains in the accepted Part V sequence,
the multi-chapter dependence item remains owned by `WD-PART`, the release-path
risk remains separately owned, and Chapter 6 remains governed by its existing
handoff. The packet therefore declares no outgoing handoff.
